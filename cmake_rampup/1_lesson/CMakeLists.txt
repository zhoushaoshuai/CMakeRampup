PROJECT(HELLO)
SET(SRC_LIST main.c submain.c)
SET(HEADER_LIST submain.h)
MESSAGE(STATUS "this is BINARY dir"${HELLO_BINARY_DIR})
MESSAGE(STATUS "this is SOURCE dir" ${HELLO_SOURCE_DIR})
ADD_EXECUTABLE(hello ${SRC_LIST} ${HEADER_LIST})
